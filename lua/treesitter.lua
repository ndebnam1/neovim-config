require'nvim-treesitter.configs'.setup {
ensure_installed = "all", -- one of "all", "maintainted" (parsers with mainters), or a list of languages
highlight = {
	enable = true, -- false will disable the enire extension
},
}
